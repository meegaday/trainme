# FitTrack 安卓版 · 出包与安装指南

> 本目录是一个 **Capacitor** 工程：把网页版 FitTrack（HTML/JS）包进原生安卓壳，产出可安装的 APK。
> 复用率 100%，云同步（Cloudflare Worker + 坚果云）原样可用。

---

## 一、本机需要装什么

| 工具 | 版本/说明 | 用途 |
|------|-----------|------|
| **Android Studio** | 最新版（Hedgehog 及以上） | 编译/运行/出包的主工具 |
| **JDK** | 17（Android Studio 自带，或单独装） | Gradle 编译需要 |
| **Android SDK** | Platform 36 + Build-Tools | 已写进 `variables.gradle`，Studio 首次打开会提示安装 |
| **Node.js** | 18+（你已有 22） | 仅在「改网页后重新同步资源」时用 |

> 不需要在本机装 Capacitor CLI——工程已经生成好，直接拿 Android Studio 打开 `android/` 即可。

---

## 二、三步出包（debug 直接装手机）

1. **打开工程**
   - Android Studio → `Open` → 选 `FitTrack-Android/android/` 文件夹。
   - 首次打开会下载 Gradle、同步依赖（约几分钟，需联网）。
2. **连设备 / 开模拟器**
   - 真机：USB 连接，手机开「开发者选项 → USB 调试」。
   - 或：Studio 里 `Device Manager` 新建一个模拟器。
3. **运行**
   - 点工具栏绿色 ▶ `Run 'app'`，选设备 → 自动编译并安装到手机。
   - 就是原生 App 了，桌面上出现 **FitTrack** 图标。

---

## 三、出正式发布版 APK / AAB

`Run` 出来的是 debug 包（不能上架）。要发布：

1. `Build` → `Build Bundle(s) / APK(s)` → `Build APK(s)`（或 `Build Bundle(s)` 用于应用商店）。
2. 第一次出 release 会要求 **签名**：
   - `Build` → `Generate Signed Bundle / APK` → 选 APK → 新建 keystore（记住密码和别名，丢失无法更新应用）。
3. 产物在 `android/app/build/outputs/apk/release/app-release.apk`。

> 版本号在 `android/app/build.gradle` 的 `versionCode` / `versionName` 改。

---

## 四、改了网页后如何更新进 App

网页资源在 `FitTrack-Android/www/`（index.html、chart.umd.min.js、xlsx.full.min.js）。
改完执行：

```bash
cd FitTrack-Android
npm install            # 仅首次
npx cap sync android   # 把 www/ 同步进 android 工程的 assets
```

然后在 Android Studio 里重新 `Run` / `Build` 即可。

> 注：在 WorkBuddy 沙箱里 `cap sync` 因环境安全删除拦截会报错，但在你本机正常。
> 只要 `www/` 内容已复制到 `android/app/src/main/assets/public/`，工程本身已可直接编译。

---

## 五、你这个 App 的关键事实

- **离线可用**：Chart.js、XLSX 已本地化到 `www/`，不依赖 CDN。只有「云同步」需要网络。
- **云同步直连坚果云**：不再经过 Cloudflare Worker。APP 内「数据」页填 **坚果云账号（邮箱）+ 应用密码**，由原生层直连 `dav.jianguoyun.com` 的 WebDAV（见下方「云同步配置」）。
- **权限**：仅 `INTERNET`（同步用）。未申请存储/定位等敏感权限。
- **最低系统**：Android 7.0（API 24）。`minSdk 24 / targetSdk 36`。
- **启动屏**：已带原生 Splash（`@drawable/splash.png`），冷启动不会白屏。
- **包名**：`com.fittrack.app`，应用名 `FitTrack`。

---

## 五之一、云同步配置（直连坚果云 WebDAV，无 Cloudflare）

网页版当初靠 Cloudflare Worker 绕过 CORS；安卓原生层用 `HttpURLConnection` 直连，CORS 不适用，因此**无需任何中转**。

1. 打开 App → **数据** 页。
2. **坚果云账号（邮箱）**：填你的坚果云登录邮箱。
3. **坚果云应用密码**：填「坚果云网页版 → 账户信息 → 安全选项 → 第三方应用管理」里生成的**应用密码**（不是登录密码）。
4. 点 **保存并测试** → 状态显示「✅ 已同步」即成功。
5. 数据文件落在坚果云根目录 `FitTrack/data.json`，可在网页版直接查看/下载备份。

安全说明：凭据（邮箱 + 应用密码）**只存在本机**（WebView 的 localStorage），不上传任何第三方。
代价：手机若被盗且未锁屏，理论上可读到该密码——个人使用可接受；要更强保护可改存 Android Keystore（后续可加）。

> 注：原 `健身管理APP/worker.js`（Cloudflare 代理）本 App 已不再使用，可忽略或删除；Cloudflare Worker 部署也可一并注销。

---

## 六、常见坑

| 现象 | 原因 | 解决 |
|------|------|------|
| 工程路径含中文导致 Gradle 失败 | 安卓工程路径必须纯 ASCII | 本工程已放到 `FitTrack-Android/`（英文），保持即可 |
| 同步后图表/导出没反应 | 忘了把 www 同步进 android | 跑 `npx cap sync android` |
| 真机点 Run 无设备 | USB 调试未开 / 驱动问题 | 手机开开发者选项+USB调试；换线/口 |
| 上架商店被拒（targetSdk 过低） | 长期未更新 | 升级 `@capacitor/android` 并调高 `targetSdkVersion` |
| 想换图标 | 替换 `android/app/src/main/res/mipmap-*` | 用 Studio `Image Asset` 工具批量生成 |
| 想换启动图 | 替换 `res/drawable*/splash.png` | 保持各分辨率尺寸 |

---

## 七、目录结构

```
FitTrack-Android/
├── www/                      # 网页源码（你平时改这里）
│   ├── index.html            # 已改为本地引用 chart/xlsx
│   ├── chart.umd.min.js      # 本地化 Chart.js
│   └── xlsx.full.min.js      # 本地化 XLSX
├── capacitor.config.json     # Capacitor 配置（appId/appName/webDir）
├── package.json
└── android/                  # 原生安卓工程（用 Android Studio 打开这个）
    └── app/src/main/
        ├── java/com/fittrack/app/MainActivity.java
        ├── java/com/fittrack/app/WebDavPlugin.java   # 直连坚果云 WebDAV 的原生插件
        ├── AndroidManifest.xml
        ├── assets/public/    # 编译后网页资源在这里
        └── res/              # 图标/启动屏/主题
```
